For running local histogram open "myMainScript.m.m" and just run, can change left_dim and right_dim as size of bin.
and for running global histogram open "myMainScript_glbal.m" and just run and change image name if want for other image.
